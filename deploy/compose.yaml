services:
  kalends:
    image: kalends:local
    container_name: kalends
    restart: unless-stopped
    ports:
      - "127.0.0.1:4180:4180"
    volumes:
      # 换成你自己的数据目录；容器以 uid/gid 10001 运行，先 chown -R 10001:10001
      - /path/to/appdata/kalends:/data
    environment:
      TZ: Asia/Shanghai
