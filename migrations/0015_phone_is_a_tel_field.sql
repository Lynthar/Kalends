-- SIM 卡的「号码」从通用文本改成电话类型：写入口据此规范化，表格里渲染成可点拨号的链接。
-- 只改类型、不动任何行数据——存量的残缺值原样保留，由界面标出来交给人判断；
-- 在迁移里批量改写别人的号码、或把打不开的旧条目拦在 400 上，都比留着残缺值更糟。
UPDATE fields SET ftype = 'tel' WHERE tbl = 'sims' AND key = 'phone_number';
